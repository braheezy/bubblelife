{
    "name": "go-opengl",
    "build": {
        "dockerfile": "../Dockerfile",
        "context": ".."
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "golang.go",
                "dtoplak.vscode-glsllint",
                "slevesque.shader"
            ]
        }
    },
    "remoteUser": "root",
    "runArgs": [
        "--rm",
        "-v",
        "/tmp/.X11-unix:/tmp/.X11-unix:rw",
        "-e",
        "DISPLAY=${env:DISPLAY}"
    ]
}
